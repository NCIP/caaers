To build this project, you need to have installed:
 - Maven
 - ServiceMix
 
All standard ServiceMix components should be installed.
 
To build, type:

	mvn install
	
To deploy, type:

	cp target/grid-servic-bc-1.0-SNAPSHOT-installer.zip $SERVICEMIX_HOME/install/
	
